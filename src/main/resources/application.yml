spring:
  datasource:
    hikari:
      maximum-pool-size: 4
    url: "jdbc:postgresql://localhost:5432/postgres"
    username: postgres
    password: "dlatl#00"
    platform: postgres

  mvc:
    pathmatch:
      # TODO 아래 설정 용도 확인
      matching-strategy: ant_path_matcher